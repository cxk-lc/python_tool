# logging.conf

[loggers]
# 打印对象, keys指定不同对象的调用时的名称
keys=root, debug, info, error, file

[logger_root]
# 指定级别, 错误、调试、信息等
level=DEBUG
handlers=debughandler, infohandler, errorhandler, filehandler

[logger_debug]
handlers=debughandler
qualname=debug
propagate=0

[logger_info]
handlers=infohandler
qualname=info
propagate=0

[logger_error]
handlers=errorhandler
qualname=error
propagate=0

# 同时输出到文件与控制台的日志对象
[logger_file]
handlers=debughandler, filehandler
qualname=debug
propagate=0

# handler用于指定日志输出位置, 可以使控制台、文件等
[handlers]
keys=debughandler, infohandler, errorhandler, filehandler

[handler_debughandler]
class=StreamHandler
level=DEBUG
formatter=form01
# 输出到控制台
args=(sys.stdout,)

[handler_infohandler]
class=StreamHandler
level=INFO
formatter=form01
args=(sys.stdout,)

[handler_errorhandler]
class=FileHandler
level=ERROR
formatter=form02
# 输出到文件，追加模式，每次不重写文件
args=('logging.log', 'a')

[handler_filehandler]
class=FileHandler
level=DEBUG
# 选择输出格式化对象
formatter=form02
# 输出到文件 w 为覆盖，a 为追加
args=('logging.log', 'a')

# 指定输出格式，可以设置多种格式，每种输出位置选定不同输出格式
[formatters]
keys=form01, form02

# 格式: 时间 文件名 代码行数 输出级别 - 输出信息
[formatter_form01]
format=%(asctime)s %(filename)s[line:%(lineno)d] - %(levelname)s - %(message)s
datefmt=%Y-%m-%d %H:%M:%S

[formatter_form02]
format=%(asctime)s %(filename)s[line:%(lineno)d] - %(levelname)s - %(message)s
datefmt=%Y-%m-%d %H:%M:%S
